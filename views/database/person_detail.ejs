<!DOCTYPE html>
  <html lang="en-US">
  <head>
    <% include ../partials/head.ejs %>
  </head>
  <body>
    <% include ../partials/header.ejs %>
    <div class="container">
      <p>
        Welcome to <%= title %>. To delete, click <a href=<%- (thePerson.url + '/delete') %>>here.</a> To update, click <a href=<%- (thePerson.url + '/update') %>>here.</a> To review the details on this person, look below.
      </p>
      <h5>THE PERSON</h5>
      <p>
        <%= (thePerson.firstName + " " + thePerson.lastName) %>
      </p>
      <h5>SHOOTS</h5>
      <ol>
        <% if (personShoots.length > 0) { for (var i = 0; i < personShoots.length; i++) {   %>
          <li>
            <%= personShoots[i].shootId %>
          </li>
        <% }} %>
      </ol>
    <% if (!personShoots | personShoots.length == 0) { %>
      <p>
        This character has no shoots.
      </p>
    <% } %>
  </div>
  <% include ../partials/foot.ejs %>
