<!DOCTYPE html>
  <html lang="en-US">
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <% include ../partials/header.ejs %>
    <div class="container">
      <p>Welcome to <%= title %></p>
      <p>
        here are the shoots currently in the database.
      </p>
      <ul>
        <% for (i=0; i<shoot_list.length; i++ ){ %>
          <li>
            <a href=
              <%= ("/database/shoot/" + shoot_list[i]._id) %>
              >
          <% if (shoot_list[i].shootId) {%>
              link to <%= shoot_list[i].shootId %>

            <% } else {%>
              link to <%= shoot_list[i]._id %>
            <% } %>
            </a>
          </li>
        <% } %>
      </ul>
    </div>
  <% include ../partials/foot %>
