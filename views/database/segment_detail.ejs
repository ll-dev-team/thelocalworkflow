<!DOCTYPE html>
  <html lang="en-US">
  <head>
    <% include ../partials/head.ejs %>
  </head>
  <body>
    <div class="container">
      <% include ../partials/header.ejs %>
      <p>Welcome to <%= title %>. To delete, click <a href=<%- theSegment.url + '/delete' %>>here.</a> To update, click <a href=<%- (theSegment.url + '/update') %>>here.</a> To review the shoot details, look below.</p>
      <p>
        <%= theSegment.fcpxml %>
      </p>
      <p>
        <%= theSegment.submissionTs %>
      </p>

      <h5>THE CLIPS</h5>
      <p>
        There are
        <%= theSegment.ioArray.length %>
         segments in this project:
      </p>
      <ol>
        <% for (i=0; i<theSegment.ioArray.length; i++) { %>
          <li>

            <%= JSON.stringify(theSegment.ioArray[i], null, 4) %>

          </li>

        <% } %>
      </ol>

    </div>
<% include ../partials/foot.ejs %>
